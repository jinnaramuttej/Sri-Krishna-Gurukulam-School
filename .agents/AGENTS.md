# Git Workflow Rule

Always automatically commit and push any changes you've made to the repository at the end of each response.
Use a descriptive commit message that summarizes the actions taken during that turn.
